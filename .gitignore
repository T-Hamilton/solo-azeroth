# your machine's settings (ports, client path, account)
server/settings.local.json
launchers/prelaunch.local.ps1

# everything built, downloaded or generated (30+ GB) - only our scripts and docs are versioned
server/azerothcore/
server/build/
server/runtime/
server/mysql/
server/deps/
server/downloads/
server/setup/*.log
server/setup/*.err
*.log
__pycache__/
local/
client-patches/build-patchZ/
client-patches/*.MPQ
client-patches/ghost-paladin/

# your bot personalities and the SQL pack built from them stay on your machine; the repo ships personalities.example.txt
server/personalities/personalities.txt
server/personalities/*.sql
server/personalities/prompts.txt

# working log, not part of the project
SUMMARY.md
